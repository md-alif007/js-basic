const num = 64;

const numSquareRoot = Math.sqrt(64);
console.log(numSquareRoot);

if (Number.isInteger(numSquareRoot)) {
    console.log(num + ' It is a perfect square');

} else {
    console.log(num + ' It is not perfect square.');

}